# T49 人間の監督・権限境界・エスカレーション方針 — 通しの記入例

改訂履歴: 2026-09-26 初版／v0.2 規模指定と懸念の引継ぎを調整／v0.5 運用標準とのクロスチェックを反映
状態: 架空作例／実案件の承認・実行・検証結果ではない
対象: AIを用いる文書検討の監督方針の架空例。将来の継続実行基盤の設計案と、今回使える権限を分ける。
適用例: 期待する記載の深さの理解、既存帳票への取り込み

[未記入テンプレート](../e-ai/T49-human-oversight.md) | [一覧](../README.md) | [共通設計](../DESIGN.md)

## 読む際の注意

この作例の人物記号、EX証拠ID、対象版、時間・費用・件数はすべて架空である。実案件へそのまま転記せず、実際の正本と許可に置き換える。参照するEX文書・証拠は架空の参照で、実ファイルはない。例の記述によって作業範囲は広がらない。5作例は別時点・別作業であり、同じIDがあっても通しの承認履歴にはならない。

## 成果物の管理（架空）

| 項目 | 記入例 |
|---|---|
| 案件・成果物 | 架空案件／EX-T49 v1 |
| 作成者・時点 | AI-Bによる報告文案／例示時点D1 |
| 状態 | 判断案。内容の承認と実行許可は未了。 |
| 正本との関係 | 本例は共通欄まで展開。他票と併用する実案件では同じ情報の正本を参照する。 |
| 変更履歴 | 初版。前版との差分なし。 |

## 共通8欄

| 欄 | 記入例 |
|---|---|
| C1 目的と期待結果 | 上位目的EX-P49 v1は、AIが許可内の作業を継続し、境界を超える判断だけを人へ渡すこと。今回は監督方針案を記述する。実行基盤への設定反映や監視の開始はしない。 |
| C2 対象と現状 | 入力は架空カードEX-W49 v1、役割表EX-ROLE01 v1、提供済みログ一覧EX-L01。権限の実測や制御部の強制機能は未確認。方針が書かれていることだけで制御が機能するとしない。 |
| C3 決定・受入責任者 | Aが目的・権限変更・費用・最終受入を判断。AI-Bが計画・実装案を作り、別主体AI-Cがレビュー・検証を兼務する役割案。Cの二役を二つの独立確認と数えない。 |
| C4 許可・禁止と費用 | EX-W49 v1は指定文書の読取・案の編集と提供済みログ確認のみ許可、30分、追加課金0円。外部送達、本番操作、権限変更、追加の試験は含まない。 |
| C5 主体と権限 | 今回のAI-Bは文書だけを変更する。将来案では制御部Dが対象・操作・上限を照合し、監視役Eが状態を通知する。D・Eが実装済み又は起動中という意味ではない。 |
| C6 検証と証拠 | 方針と作業カードの矛盾を机上で自己確認する。拒否経路、時間・費用制御、再開時の版照合は未評価。将来の検証項目であって今回の追加実行指示ではない。 |
| C7 停止・復旧 | 将来案は『既存許可内かつ上限内なら継続、範囲外・上限到達・安全な手段なしなら該当操作を止めAへ判断依頼』とする。標準6.11の設計復帰条件も保持する。許可がなくなった操作を別経路で再開しない。 |
| C8 結果と残課題 | 役割・操作・判断先を下表の方針案に整理した想定。強制機構は未確認で、有効性を保証しない。Aが方針を判断し、必要な制御設計・検証は後続の許可範囲で扱う。 |

## 固有欄

| 欄 | 記入例 |
|---|---|
| 判断と操作 | Bは未達条件と選択肢を整理する。将来のDは既存許可との照合を担い、Aは範囲変更・上限変更・最終受入を決める。監視役Eは完了を決めない。 |
| 許可と禁止 | 指定文書の編集と提供済みログの確認はEX-W49内。外部に新しい証拠を取りに行く操作、追加テスト、本番修正は含まない。レビュー指摘だけでは許可を追加しない。 |
| 閾値 | 今回の文書検討は30分・追加課金0円という架空条件。将来の再試行上限は未設定のため継続実行の設定を確定しない。運用標準6.11を緩める独自閾値を置かない。 |
| 停止と継続 | 証拠不足なら完了判定をしない。ただし既存ログから不足を整理する作業は許可内で続ける。必要な操作が範囲外なら、その操作と依存する判断を保留する。 |
| 判断依頼 | Aへ、事実状態・未達条件・実施済み対処・選択肢・影響・推奨を渡す。承認後も対象版と許可範囲を確認し、古い許可を別版へ流用しない。 |
| 兼務 | Cによるレビューと検証は実装役Bから別主体であるという設定だが、Cの中では独立ではない。機械的検査、実経路の証拠、Aの受入を、それぞれ別の根拠として扱う。 |

## 明細

| 行為 | 主体 | 許可範囲 | 上限 | 境界時の判断先 | 再開条件 |
| --- | --- | --- | --- | --- | --- |
| 提供済みログの確認 | B | EX-W49 v1に指定済み | 文書検討30分内 | 範囲外の入力が必要ならA | 対象版・入力範囲を再確認 |
| 新しい検証の実施 | B又はCの候補 | 今回未許可 | 未設定 | A | 範囲・費用・証拠計画が別途承認されること |
| 進捗の通知 | 将来案E | 方針案のみ・未起動 | 未設定 | A | 監視手段と通知先の許可を確定 |
| 最終受入 | A | 人間の責任者の判断 | 案件の条件に従う | A | 必要な証拠が揃い、未評価範囲を扱えること |

## 障害・課題・懸念と引継ぎ

| 障害・課題・懸念／未決 | 根拠・判断への影響 | 担当・判断時点 | 次へ渡す入力 | 継続可能／未許可 |
|---|---|---|---|---|
| 制御の有効性が未確認 | 方針だけでは逸脱を防げたと証明できない | A／制御設計の受入前 | EX-W49 v1と本方針案 | 文書照合は可能。拒否試験・設定変更は今回未許可。 |
| 将来の継続実行上限 | 再試行・監視を起動できない | A／継続実行の開始前 | 上限欄と標準6.11 | 上限候補の記録まで。自動実行・監視の開始は未許可。 |

## この作例で確認すること

- 監督という名目で全操作の再承認を要求していないか：上の共通欄・固有欄・明細を対応させて読む。
- 未確認を隠す代わりに、判断への影響と担当・時点を示している。
- 記載の厚みを保ちながら、追加実行を許可せず、既存の許可内の整理は止めていない。
- 作例の体裁を満たしても、実案件の証拠、権限、受入の代替にはならない。

出典の観点は[対応テンプレートの参考文献](../e-ai/T49-human-oversight.md)と[参考文献台帳](../REFERENCES.md)を参照。物語・数値・記入内容は本集の独自作例である。
